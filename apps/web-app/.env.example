VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Example: https://<project-ref>.supabase.co/functions/v1/api-web
VITE_API_BASE=

# Server-only settings such as WEB_ALLOWED_EMAILS, AI keys, Google credentials,
# and VAPID private keys belong in the root .env.local / Supabase secrets.
# Never expose them through VITE_* variables.
